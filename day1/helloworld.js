// call a function on each element of an array using forEach
let colors = ["red", "orange", "yellow", "green", "blue", "indigo", "violet"];
colors.forEach((color) => console.log("dying hair:", color));
colors.map((color) => color.toUpperCase()).forEach((color) => console.log("dying hair:", color));